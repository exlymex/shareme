import React from 'react';
import {IUser} from "../../types";
interface IProps{
    user:IUser | null
}
const CreatePin = ({user}:IProps) => {
    return (
        <div>
            
        </div>
    );
};

export default CreatePin;
